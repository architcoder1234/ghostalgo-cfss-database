const q=document.getElementById("search"),f=document.getElementById("filter"),cat=document.getElementById("catalog"),count=document.getElementById("count");const edits=JSON.parse(localStorage.getItem("cfss_edits")||"{}");const esc=s=>String(s||"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll('"',"&quot;");function render(){let rows=CFSS_CARS.filter(c=>(f.value=="all"||c.category==f.value)&&c.name.toLowerCase().includes(q.value.toLowerCase())).map(c=>({...c,...(edits[c.id]||{})}));cat.innerHTML=rows.map(c=>`<article class="card">${c.image?`<img class="pic" src="${c.image}" alt="${esc(c.name)}">`:`<div class="pic">🚗</div>`}<div class="body"><span class="tag">${esc(c.category)}</span><h3>${esc(c.name)}</h3>${c.buy||c.sell?`<p>${c.buy?"Buy: "+esc(c.buy):""}${c.buy&&c.sell?" • ":""}${c.sell?"Sell: "+esc(c.sell):""}</p>`:""}${c.notes?`<p>${esc(c.notes)}</p>`:""}</div></article>`).join("")||"<p>No cars found.</p>";count.textContent=rows.length+" cars"}[...new Set(CFSS_CARS.map(c=>c.category))].forEach(x=>{let o=document.createElement("option");o.value=x;o.textContent=x;f.appendChild(o)});q.oninput=render;f.onchange=render;render();